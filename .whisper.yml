provider:
  type: vault
  address: http://localhost:8200

  # userpass authentication
  # authMethod: userpass
  # authMount: userpass

  # OIDC authentication
  authMethod: oidc
  authMount: oidc
  secretMount: secret
  callbackPort: 8250
secrets:
- path: mysecret
  prefix: MY_APP_
